<UserControl x:Class="ProjectLex.InventoryManagement.Desktop.Controls.ProcessingOrderCountControl"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectLex.InventoryManagement.Desktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <materialDesign:Card Style="{StaticResource MaterialDesignElevatedCard}" Background="{DynamicResource MaterialDesignPaper}">
        <Grid Margin="20 20 20 20">
            <Label FontFamily="Sogoe UI" FontWeight="Bold" HorizontalAlignment="Center">
                <StackPanel>
                    <Label Content="PROCESSING" FontSize="18" Style="{DynamicResource  MaterialDesignLabel}"/>
                    <TextBlock Text="{Binding ProcessingOrdersCount}" Style="{StaticResource MaterialDesignTextBlock}" FontSize="24" VerticalAlignment="Center"/>
                </StackPanel>
            </Label>

        </Grid>
    </materialDesign:Card>
</UserControl>
